.private_env.sh
**/private/env
**/.config/private/

**/.config/nix-private/

.env.local
.env.private
**/.secrets/
.serena/memories/serena_tool_compatibility_nix.md
.serena/
.claude/settings.local.json
.claude/memory/
.claude/plans/
# `skills add` mirrors .agents/skills into .claude/skills as symlinks.
# The real skills live in .agents/skills and are edited in place here,
# so this mirror is derived state, not a second source of truth.
.claude/skills/
.cunzhi-memory/
.spec-workflow/
.trellis/

# Reference repositories (auto-managed)
.ref/
!.ref/README.md
!.ref/*.example.sh

# Personal notes (not shared)
notes/
nixos-niri-dms-vm.qcow2
nixos-niri-vm.qcow2
result
result-dms
result-nixos-niri-dms-vm
result-hm-gen
secrets/vm-key.txt

# Nix flake check artifacts
791040dst
791040src

# Generated by git-hooks.nix devshell on `nix develop`
.pre-commit-config.yaml

# Research / planning docs (not config)
PLAN.md
docs/den/

# Pix Agent
.pi/
.pi-lens/

# `skills` CLI install manifest. This repo's skills are authored and edited
# in place, not synced from upstream, so the manifest tracks nothing useful.
skills-lock.json
